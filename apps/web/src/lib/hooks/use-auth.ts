/**
 * @deprecated Import from `@/lib/auth` instead.
 * Re-exports for backward compatibility during Phase 1 migration.
 */
export {
  useCurrentUser,
  useLoginWithEmail,
  useSignupWithEmail,
  useLoginWithTelegram,
  useLogout,
} from "@/lib/auth";
